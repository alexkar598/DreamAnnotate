name: DreamAnnotate
author: alexkar598
description: Parse and annotate the output of dreamcheck
runs:
  using: "composite"
  steps:
    - run: "echo ::add-matcher::${{ github.action_path }}/matcher.json"
      shell: bash
branding:
  icon: file-text
  color: orange
